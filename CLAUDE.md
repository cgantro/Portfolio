# Portfolio Project

## 목표

개발자 취업용 포트폴리오 사이트 제작

## 핵심 메시지

"기능 구현보다 시스템 문제를 분석하고 구조적으로 해결"

## 기본 규칙

- 프로젝트 데이터 하드코딩 금지 → `data/projects/` 사용
- 기능 나열 금지 → 성능 수치 / 문제 해결 스토리 우선
- 컴포넌트 재사용 원칙
- 모바일 우선 (mobile-first)

## 자동 위임 규칙

| 작업 | subagent |
|------|----------|
| 레포 분석 → 재료 수집 (01~08) | `repo-analyzer` |
| 서사 조립 → 검수 완성 (09~11) | `portfolio-builder` |
| UI 규칙 검수 | `ui-reviewer` |
| 컴포넌트 구조 설계 | `architecture-planner` |

## 프로젝트 분석 파이프라인

새 프로젝트 분석 시 순서대로 호출:

```
01 → 02 → 03+04+05 → 06+07+08 → 09 → 10 → 11
(수집)  (맥락)  (구조화)     (심화)      (집필) (다듬기) (검수)
```

단계를 건너뛰면 다음 스킬의 input이 부족해진다.

필요한 skill은 현재 작업과 관련된 것만 사용.
모든 skill을 한꺼번에 읽지 말 것.
